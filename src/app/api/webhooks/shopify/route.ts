import { NextRequest, NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { hashPayload, safeShopDomain, verifyWebhook } from "@/lib/security/shopify";
export async function POST(request: NextRequest) {
  const raw = await request.text(); if (!verifyWebhook(raw, request.headers.get("x-shopify-hmac-sha256"))) return NextResponse.json({ error: "Invalid webhook signature" }, { status: 401 });
  try { const domain = safeShopDomain(request.headers.get("x-shopify-shop-domain") || ""); const webhookId = request.headers.get("x-shopify-webhook-id"); const topic = request.headers.get("x-shopify-topic"); if (!webhookId || !topic) return NextResponse.json({ error: "Invalid webhook headers" }, { status: 400 }); const payload = JSON.parse(raw) as Record<string, unknown>; const admin = createAdminClient(); const { data: shop } = await admin.from("shops").select("id,organization_id").eq("shop_domain", domain).maybeSingle(); if (!shop) return new NextResponse(null, { status: 200 }); const { error } = await admin.from("webhook_events").insert({ webhook_id: webhookId, shop_id: shop.id, topic, api_version: request.headers.get("x-shopify-api-version"), payload_hash: hashPayload(raw), payload }); if (error?.code === "23505") return new NextResponse(null, { status: 200 }); if (error) throw error; const kind = topic.startsWith("orders/") ? "orders" : topic.startsWith("products/") ? "products" : "fulfillments"; await admin.from("sync_jobs").insert({ shop_id: shop.id, kind }); return new NextResponse(null, { status: 200 }); } catch { return NextResponse.json({ error: "Webhook processing failed" }, { status: 500 }); }
}
