import Link from "next/link";
import { Activity, Package, Settings } from "lucide-react";
import { cn } from "@/lib/utils";
const navigation = [{ href: "/orders", label: "Orders", icon: Package }, { href: "/dispatched", label: "Dispatched", icon: Activity }, { href: "/settings", label: "Settings", icon: Settings }];
export function AppShell({ active, children }: { active: string; children: React.ReactNode }) { return <div className="min-h-dvh bg-slate-50 md:flex"><aside className="hidden w-60 shrink-0 border-r border-slate-200 bg-white p-5 md:block"><p className="mb-8 text-lg font-bold">Dispatch</p><nav className="space-y-1">{navigation.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={cn("flex min-h-11 items-center gap-3 rounded-xl px-3 text-sm font-semibold", active === label ? "bg-slate-950 text-white" : "text-slate-600 hover:bg-slate-100")}><Icon size={18}/>{label}</Link>)}</nav></aside><main className="mx-auto w-full max-w-6xl px-4 pb-24 pt-5 md:px-8 md:pb-8">{children}</main><nav className="safe-bottom fixed inset-x-0 bottom-0 z-20 flex border-t border-slate-200 bg-white/95 px-3 pt-2 backdrop-blur md:hidden">{navigation.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={cn("flex flex-1 flex-col items-center gap-1 rounded-lg py-2 text-xs font-semibold", active === label ? "text-slate-950" : "text-slate-400")}><Icon size={19}/>{label}</Link>)}</nav></div>; }
