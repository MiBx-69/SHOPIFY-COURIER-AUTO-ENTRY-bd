"use client";
import { useState } from "react";
import { KeyRound, Mail } from "lucide-react";
import { Button } from "@/components/ui/button";
import { passkeyService } from "@/lib/auth/passkeys";
import { createClient } from "@/lib/supabase/client";
export function LoginForm() { const [email, setEmail] = useState(""); const [message, setMessage] = useState<string>(); const [busy, setBusy] = useState(false); async function passkey() { setBusy(true); setMessage(undefined); const { error } = await passkeyService.signIn(); if (error) setMessage(error.message); else window.location.assign("/orders"); setBusy(false); } async function magicLink(e: React.FormEvent) { e.preventDefault(); setBusy(true); const { error } = await createClient().auth.signInWithOtp({ email, options: { emailRedirectTo: `${window.location.origin}/auth/callback` } }); setMessage(error ? error.message : "Check your email for a secure sign-in link."); setBusy(false); } return <section className="w-full rounded-3xl bg-white p-7 shadow-sm ring-1 ring-slate-200"><p className="mb-2 text-sm font-semibold text-slate-500">SHOPIFY DISPATCH</p><h1 className="text-3xl font-bold tracking-tight">Move orders, confidently.</h1><p className="mt-3 text-sm leading-6 text-slate-600">Use your passkey for the fastest secure sign-in.</p><Button className="mt-7 flex w-full items-center justify-center gap-2" onClick={passkey} disabled={busy}><KeyRound size={18}/>Continue with Passkey</Button><div className="my-6 flex items-center gap-3 text-xs text-slate-400"><span className="h-px flex-1 bg-slate-200"/>or<span className="h-px flex-1 bg-slate-200"/></div><form onSubmit={magicLink} className="space-y-3"><label className="block text-sm font-medium">Email fallback<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@company.com" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-3"/></label><Button variant="secondary" className="flex w-full items-center justify-center gap-2" disabled={busy}><Mail size={17}/>Email me a sign-in link</Button></form>{message && <p role="status" className="mt-4 rounded-xl bg-slate-100 p-3 text-sm text-slate-700">{message}</p>}</section>; }
